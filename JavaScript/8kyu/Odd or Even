function oddOrEven(array) {
  if (array.length > 0) {
    let result = array.reduce(function(sum, elem) {
	    return sum + elem;
    });
    return result % 2 === 0 ? 'even' : 'odd';   
  } else {
    return 'even'
  } 
}
