function findShort(s){
  let arr = s.split(' ')
  let minWordLength = arr[0].length
  for (i=0; i < arr.length; i++) {
    if (arr[i].length < minWordLength) {
      minWordLength = arr[i].length
    }    
  }
  return minWordLength
}
