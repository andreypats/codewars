function sumMix(x){
  let sum = 0;
  for (let i = 0; i < x.length; i++) {
    x[i] === Number ? sum += x[i] : sum += Number(x[i])  
  }
  return sum;
}

function sumMix(x){
  return x.map(a => +a).reduce((a, b) => a + b);
}
