function openOrSenior(data){
  let newData = [];
  for (let i=0; i < data.length; i++) {
    data[i][0] >= 55 && data[i][1] >7 ? newData.push('Senior') : newData.push('Open');    
  }
  return newData;
}
